//very similar to tutorial
angular.
  module('myApp').
  component('eventsView', {
    templateUrl: 'events-view.html',
    controller:
      function eventsViewController($routeParams, $scope, $rootScope) {

              //name, list, location from events table
              //signin names from sign ins table 


      // for (var id in obj) {
      //   if (obj.hasOwnProperty(prop)) {
      //
      //   }
      // }




      }

  });
